# FinTrack

Personal finance management app built with Next.js, Clerk, Neon PostgreSQL, and Drizzle ORM.

![Next.js](https://img.shields.io/badge/Next.js-16-black?logo=next.js)
![React](https://img.shields.io/badge/React-19-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?logo=typescript)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4-06B6D4?logo=tailwindcss)
![Clerk](https://img.shields.io/badge/Clerk-latest-6C47FF?logo=clerk)
![Drizzle](https://img.shields.io/badge/Drizzle_ORM-latest-C5F74F?logo=drizzle)

---

## Features

- **Authentication** — Sign-in and sign-up powered by Clerk
- **User sync** — Automatic Clerk-to-local database user synchronization
- **Categories** — Customizable expense and income categories with emojis and colors
- **Transactions** — Track expenses and income with descriptions, dates, and notes
- **Budgets** — Set monthly or yearly budgets per category
- **Serverless database** — PostgreSQL via Neon with Drizzle ORM

## Tech Stack

| Layer | Technology |
|---|---|
| Framework | Next.js 16 (App Router) |
| UI | React 19, Tailwind CSS 4 |
| Language | TypeScript 5 (strict) |
| Auth | Clerk |
| Database | Neon (serverless PostgreSQL) |
| ORM | Drizzle ORM + `drizzle-orm/neon-http` |
| Package manager | pnpm |

## Getting Started

### Prerequisites

- Node.js >= 20
- pnpm
- A [Neon](https://neon.tech) PostgreSQL database
- A [Clerk](https://clerk.com) application

### Environment Variables

Create a `.env` file in the project root:

```env
DATABASE_URL="postgresql://..."
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
```

### Install & Run

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

### Database Migrations

```bash
pnpm drizzle-kit generate   # Generate SQL migration
pnpm drizzle-kit migrate    # Apply migration to Neon
```

## Project Structure

```
├── app/                    # Next.js App Router pages and layouts
│   ├── (auth)/             # Authentication pages (sign-in, sign-up)
│   ├── layout.tsx          # Root layout with ClerkProvider
│   ├── page.tsx            # Landing page
│   └── globals.css         # Tailwind CSS entry point
├── components/             # React components
├── db/                     # Database layer
│   ├── schema.ts           # Drizzle table definitions and relations
│   └── migrations/         # SQL migrations (generated by drizzle-kit)
├── lib/                    # Shared utilities
│   ├── db.ts               # Neon + Drizzle connection
│   ├── default-categories.ts  # Preset expense/income categories
│   └── getOrCreateUser.ts  # Clerk-to-local user sync helper
├── public/                 # Static assets
├── proxy.ts                # Clerk middleware (route protection)
├── drizzle.config.ts       # Drizzle Kit configuration
└── next.config.ts          # Next.js configuration
```

## Database Schema

| Table | Purpose |
|---|---|
| `users` | Local user profiles synced from Clerk |
| `categories` | Expense/income categories with icon and color |
| `expenses` | Individual transactions (amount, date, description, notes) |
| `budgets` | Monthly or yearly budget limits per category |

Relations: `users` has many `categories`, `expenses`, and `budgets`. `categories` has many `expenses` and `budgets`.

## Scripts

| Command | Description |
|---|---|
| `pnpm dev` | Start development server |
| `pnpm build` | Production build |
| `pnpm start` | Start production server |
| `pnpm lint` | Run ESLint |
| `pnpm drizzle-kit generate` | Generate database migration |
| `pnpm drizzle-kit migrate` | Apply pending migrations |
